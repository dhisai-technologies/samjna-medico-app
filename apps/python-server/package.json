{
  "name": "python-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "source venv/bin/activate && uvicorn app.main:app --reload --port 8002"
  },
  "devDependencies": {
    "@tools/typescript-config": "workspace:*"
  }
}