{
  "name": "python-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "source venv/bin/activate && fastapi dev app/main.py"
  },
  "devDependencies": {
    "@tools/typescript-config": "workspace:*"
  }
}